import CountryTemplate from '@/components/templates/CountryTemplate';
import { turkeyData } from '@/data/destinations-data';

const Turkey = () => {
  return <CountryTemplate country={turkeyData} />;
};

export default Turkey;
